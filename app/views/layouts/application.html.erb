<!DOCTYPE html>
<html>
  <head>
    <title>Stock Trading App of some sorts | Trading at your fingertips</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-white dark:bg-gray-900 py-8 px-8">
    <%# Flash %>
    <% unless flash.empty? %>
    <div id='flash'>
      <script type="text/javascript">
          <% flash.each do |f| %>
            <% type = f[0].to_s.gsub('alert', 'error').gsub('notice', 'success') %>
            toastr['<%= type %>']('<%= f[1] %>', '', { closeButton: true, progressBar: true, newestOnTop: false, positionClass: 'toast-bottom-right' });    
          <% end %>
      </script>
    </div>
    <% end %>
    
    <%= render "layouts/header" %>
    <div class="relative pt-36">
      <%= yield %>
    </div>
    
  </body>
</html>